# Global memory cap for the whole user session (user-manager root slice).
# MemoryHigh throttles and reclaims into swap; MemoryMax is the hard backstop. Tune to RAM.
[Slice]
MemoryHigh=52G
MemoryMax=56G
